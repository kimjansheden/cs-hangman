namespace Inlämningsuppgift_Hangman
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Console.Clear(); //To start the console application without the extra output in the beginning (file path, exit code)
            Play.Start();
        }
    }    
}